apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webformchart.fullname" . }}-test-connection"
  labels:
    {{- include "webformchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webformchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
