apiVersion: v1
kind: Service
metadata:
  name: {{ include "webformchart.fullname" . }}
  labels:
    {{- include "webformchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webformchart.selectorLabels" . | nindent 4 }}
